# Codec_HandwrittenDigitRecognizer
This project develops a Handwritten Digit Recognition system using a Convolutional Neural Network (CNN) and the MNIST dataset. The model learns image patterns from handwritten digits (0–9) and classifies them accurately. The project demonstrates image processing, deep learning, neural networks, and model evaluation using TensorFlow and Keras.
